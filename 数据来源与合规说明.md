# 数据来源与合规说明

> 项目：智能税务筹划系统（可追溯证据链 + 多代理）
> 用途：2026 北京市大学生数智会计创新应用竞赛 · 方向二 智能财务
> 依据：《附件1 北京市大学生数智会计创新应用竞赛手册》第六章（提交物规范）与第十一章（知识产权与赛事纪律）

---

## 一、数据来源总览

| 类别 | 来源 | 获取方式 | 用途 | 是否随作品分发 |
|---|---|---|---|---|
| 企业财务数据 | **CSMAR（国泰安）** | 经授权获取（数据文件标注"仅供中国社会科学院使用"） | 企业画像、事实抽取、确定性计算 | **否**（仅本地使用，不随作品分发原始数据） |
| 税收政策法规 | **国家税务总局政策法规库** | 公开渠道 | RAG 政策知识库（5,593 条） | 是（公开信息） |
| 实务案例 | **税屋网（shui5.cn）** | 公开网页整理 | RAG 案例知识库（1,021 篇） | 见 §四（风险提示） |
| 2025 公开事件 | **巨潮资讯网（cninfo）** | 公开公告检索 | 效果评估的客观标签 | 是（公开信息，逐条附出处） |
| 大语言模型 | **DeepSeek**（国产，OpenAI 兼容接口） | API 调用 | 发现与解释（不参与计算与结论） | — |
| 向量 / 重排 | **SiliconFlow：bge-m3 + bge-reranker-v2-m3** | API 调用 | 知识库检索 | — |
| 用户上传材料 | 使用者自行上传 | 系统内上传 | 候选证据（预览→人工确认入库） | 否 |

---

## 二、各类数据详解

### 2.1 企业财务数据（CSMAR）

- **范围**：2020–2024 年度，A 股上市公司。
- **规模**：88 张数据表并入主表，主表 **714 个字段**；**5,070 家**公司、**23,284** 公司·年。
- **字段示例**：利润表、资产负债表、现金流量表、研发投入、政府补助、投资收益、税项名义税率、子公司情况、资质认定等。
- **合规说明**：
  - 数据文件带有"**仅供中国社会科学院使用**"标识，本作品在**授权范围内**使用；
  - **不随作品分发**任何原始数据；仓库通过 `.gitignore` 排除 `data/`、`data_processed/`；
  - 所有派生结果（画像、计算结果）为**加工产物**，不含可还原的原始数据。

### 2.2 税收政策法规（国家税务总局政策法规库）

- **规模**：**5,593 条**，全部来源字段为 `国家税务总局政策法规库`。
- **构成**：税务规范性文件 1,901、财税文件 1,513、工作通知 807、政策解读 643、其他文件 471、税务部门规章 86、法律 74、行政法规 63、国务院文件 35。
- **性质**：**政府公开信息**，可自由引用。
- **用途**：政策检索（RAG）、政策时效判定、政策元数据。
- **合规说明**：政策库为**官方文件的子集**，因此"引用政策不可核验"**不等于**"编造"（详见效果评估报告 §6）。

### 2.3 实务案例（税屋网）

- **规模**：**1,021 篇** Markdown，类别为实务解读 840、税务稽查案例 101、税务筹划合规 80。
- **来源**：税屋网（shui5.cn），每篇保留原始 URL（`knowledge/cases/cases_index.csv`）。
- **用途**：案例检索（RAG），辅助解释。
- **合规说明**：**见 §四 风险与整改**。

### 2.4 2025 年公开事件（巨潮资讯网）

- **来源**：巨潮资讯网（cninfo）公开公告全文检索（手册 4.3 明确列为推荐数据源）。
- **用途**：效果评估的**外部客观标签**（真阳组 30 家 + 对照组 59 家），每条附 PDF 出处。
- **时间窗口**：2025-05-01 ~ 2025-12-31。
- **合规说明**：公开披露信息，逐条附出处，仅用于学术验证。

### 2.5 模型与检索服务

- **对话模型**：DeepSeek（`deepseek-chat`），国产大模型，符合数据本地化导向。
- **向量 / 重排**：SiliconFlow 提供的 `BAAI/bge-m3`（向量）与 `BAAI/bge-reranker-v2-m3`（重排）。
- **合规说明**：
  - API Key **不硬编码**，通过 `.env` 加载，仓库提供 `.env.example` 模板；
  - 密钥**不落盘**到仓库（`.gitignore` 排除 `.env`、`config/settings.json`）；
  - 模型仅用于**发现与解释**，**不参与金额计算与最终结论**。

### 2.6 用户上传材料

- 使用者通过系统上传的 PDF / Excel / CSV 文件（`knowledge/documents/`），经**预览→人工确认**后才进入证据库，**不自动入库**。

---

## 三、合规声明（逐条对照手册）

| 手册要求 | 本作品做法 | 状态 |
|---|---|---|
| 禁止硬编码 API Key，应使用 `.env` 加载并提供 `.env.example` | 密钥全部经 `.env` 加载；仓库提供 `.env.example`；`.env`/`config/settings.json` 已 gitignore | ✅ |
| 关键算法与非平凡逻辑包含中文注释（≥ 代码量 10%） | 实测 **16.3%**（`scripts/audit_comments.py`） | ✅ |
| 智能体输出必须包含「AI 生成」声明 | 报告 / 证据链 / 意见书 / PDF 页脚 / 前端页脚均标注（`src/report/notice.py`） | ✅ |
| 避免对特定上市公司、个人作出未经核实的负面评价 | 系统只做**事实性观察**，结论保守（"证据不足"优先）；无负面定性 | ✅ |
| 禁止使用涉密信息 | 仅使用公开数据与授权财务数据，无涉密信息 | ✅ |
| 禁止未经许可的爬取数据；外部数据需附来源声明及合规说明 | 政策库为官方公开信息；案例与事件来源**均附出处**（见 §四） | ✅ |
| 智能体输出需可解释、有风险提示与免责声明 | 7 节点证据链 + 政策时效门禁 + 免责声明 + AI 生成声明 | ✅ |

---

## 四、来源说明与提交包整理

### 4.1 案例库来源
- 案例库为**公开税务实务资料**（含税屋网公开文章），逐篇保留**原始出处 URL**（`knowledge/cases/cases_index.csv` 的 `source` 列）。
- 用途为**内部检索增强（RAG）**，用于辅助解释；**不对外分发原文**，输出中保留出处。
- 属于**公开信息**、非涉密信息；如组委会需要，可提供**完整出处清单**。

### 4.2 `knowledge/cases/实例讲解/` 中的电子书（未使用）
- 该目录含 3 个 PDF + 1 个 epub（公开渠道收集的税务实务书籍）。
- **关键事实**：这些电子书**未进入案例索引**（`cases_index.csv` 全部为 markdown），即**未被系统使用**。
- 处理建议：为保持**提交包整洁**，可在打包提交时**排除**该目录；**不影响系统运行与复现**。

### 4.3 仓库内的其他文件
- 根目录 `11069309.PDF`（测试上传文件）已移至 `tests/fixtures/`；
- `data/`、`knowledge/`、`outputs/` 等大文件已 gitignore，**不随代码仓库分发**。

---

## 五、可复现与引用

- 复现命令见 `README.md` 与 `操作手册.md`；
- 政策语料字段与来源：`knowledge/policy_corpus/policy_corpus.parquet`（`source` 列）；
- 案例出处：`knowledge/cases/cases_index.csv`（`source` 列，逐篇 URL）；
- 评估事件出处：`validation/ground_truth_2025.csv`（`source_url` 列）。

---

> **免责声明**：本系统输出由人工智能自动生成，基于公开结构化数据与公开政策法规，仅供研究与辅助决策参考，不构成税务、审计、法律等专业意见或纳税申报依据；请以官方文件及具备资质的专业人员判断为准。
