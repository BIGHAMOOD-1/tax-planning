# 智能税务筹划系统 · 技术报告

| 文档信息 | |
|---|---|
| 项目名称 | 智能税务筹划系统（可追溯证据链 + 多代理） |
| 参赛赛道 | 2026 北京市大学生数智会计创新应用竞赛 · 方向二 智能财务（税务筹划 / 涉税合规） |
| 文档性质 | 技术报告（初赛评审提交物） |
| 版本 | v1.0 |
| 主编程语言 | Python 3.11（后端） / TypeScript + React（前端） |
| 核心模型 | DeepSeek（对话）、bge-m3 + bge-reranker-v2-m3（检索） |

---

## 目录

1. [项目概述](#1-项目概述)
2. [问题定义与设计原则](#2-问题定义与设计原则)
3. [系统架构](#3-系统架构)
4. [核心机制](#4-核心机制)
5. [数据与知识库](#5-数据与知识库)
6. [工程实现](#6-工程实现)
7. [测试与效果评估](#7-测试与效果评估)
8. [合规与伦理](#8-合规与伦理)
9. [复现与运行](#9-复现与运行)
10. [局限与展望](#10-局限与展望)

---

## 1. 项目概述

本系统面向企业税务筹划与涉税合规场景，构建一个**可追溯、可核验、结论保守**的多智能体分析系统。它接收上市公司的结构化财务数据，自动产出**税务方向分析、7 节点证据链、确定性计算与合规税务意见**，并支持报告 / 证据链 / 意见书的 Markdown 与 PDF 导出。

**核心定位**：**程序做确定性判定与计算，大模型只做发现与解释**。系统的价值不在于"猜得更准"，而在于**每一条建议都可核验、有证据链、受政策时效约束**。

---

## 2. 问题定义与设计原则

### 2.1 为什么不能"让大模型直接回答"

通用大模型在税务场景存在三个**结构性缺陷**：
1. **不可核验**：无法追溯结论到企业具体数据；
2. **政策不可靠**：可能引用不存在或已失效的政策（本系统评估中观察到编造文号，如"国家税务总局公告2025年第X号"）；
3. **过度声称**：对不具备条件的公司也给出"可享受"的结论，带来税务风险。

### 2.2 设计原则

| 原则 | 含义 |
|---|---|
| **确定性优先** | 金额与结论由确定性计算器与门禁产出，可复现（同输入同输出） |
| **证据为王** | 一切结论必须可追溯到企业事实与现行有效政策 |
| **宁可保守** | 证据不足时输出 `DATA_GAP`，而非编造结论 |
| **口径分离** | 事实的"来源"与"采用"分离；金额的"语义"与"数值"分离 |
| **人工兜底** | 证据冲突、政策时效不明等情形交由人工复核 |

---

## 3. 系统架构

### 3.1 总体分层

```
数据层    → 88 张 CSMAR 表 → 主表(714 字段) → 企业画像(845 字段)
   │
知识层    → 政策库(5,593) + 案例库(1,021) → 向量 + BM25 + Reranker 混合检索
   │
证据层    → 证据契约 / 采用 / 冲突 / 复核 / 血缘（事实三维：来源·校验·采用）
   │
技能层    → 24 个 Tax Skill（规则 + 条件 + 计算器 + 政策 + 证据要求）
   │
代理层    → 机会分析(Red) / 合规审查(Blue) / 综合决策(Green) / 税务顾问(Advisor)
   │
产出层    → 结论四态 + 7 节点证据链 + 报告 / 导出（Markdown / PDF）
```

### 3.2 多代理协作

| 代理 | 职责 | 是否参与结论 |
|---|---|---|
| **Red（机会分析）** | 从企业数据中发现筹划方向与潜在收益 | 否（仅发现） |
| **Blue（合规审查）** | 审查红方提案的合规边界与风险 | 否（仅审查） |
| **Green（综合决策）** | 结合规则与辩论结果，产出**最终结论与理由** | **是** |
| **Advisor（税务顾问）** | 生成"合规税务意见"（适用条件、实施路径、资料清单、风险红线） | 否（意见书） |

> 辩论机制：Red 与 Blue 就每个方向进行多轮对抗，Green 汇总。**结论始终受确定性计算与政策时效门禁约束**。

### 3.3 端到端流程

```
加载画像 → 证据池 → Discovery 三级分类 → Gate 门禁 → 深度分析（Red/Blue/Green）
        → 确定性计算 → 政策时效门禁 → 结论四态 → 证据链 / 报告 / 意见书
```

---

## 4. 核心机制

### 4.1 证据层：事实的三维刻画

| 维度 | 取值 | 含义 |
|---|---|---|
| **分辨率** `resolution` | DIRECT / PROXY / INSUFFICIENT / MISSING | 该事实与口径的直接程度 |
| **校验状态** `verification_status` | candidate / confirmed / rejected | 证据是否经人工确认 |
| **采用状态** `resolution_status` | pending / accepted / superseded / conflict | 是否被当前画像采用 |

- **来源优先级**：`manual(100) > user_structured(80) > csmar(60) > user_document(40)`；**只补空不覆盖**（manual 例外）。
- **口径优先级**：`INSUFFICIENT/MISSING > PROXY > DIRECT`。
- **按技能语境覆盖**（`config/fact_resolution.yaml: by_skill`）：同一事实在不同方向下口径不同（如利润总额在高企下为 DIRECT、在亏损弥补下为 PROXY）。

### 4.2 金额语义：`calculation_type` 与不变量

系统严格区分**六类金额语义**：

| `calculation_type` | 含义 |
|---|---|
| BASELINE_TAX | 基线税负 |
| SCENARIO_TAX | 情景税负 |
| INCREMENTAL_TAX_BENEFIT | 新增税收收益 |
| TAX_SHIELD | 税盾（单独呈现） |
| NOT_CALCULABLE | 不可计算 |
| NO_CALCULATION | 无需计算 |

**系统不变量（硬约束）**：
```
confirmed_tax_impact  ⇒  calculation_type == INCREMENTAL_TAX_BENEFIT
                          ∧ direction == tax_reduce
                          ∧ 恒 ≥ 0
```
- `tax_increase` 仅作**风险提示**，不进入确认 / 情景字段；
- 汇总**不跨方向净额**（避免"正负相抵"的误导）。

### 4.3 政策时效门禁

政策时效 `policy_validity ∈ {VALID, EXPIRED, NOT_YET_EFFECTIVE, UNKNOWN}`。

| 时效 | 处理 |
|---|---|
| VALID | 放行 |
| EXPIRED / NOT_YET_EFFECTIVE | **撤销确认影响并降级** |
| UNKNOWN | **不得单独支撑 CONFIRMED** |

> 该门禁使"引用失效政策"在结构上难以发生（评估中本系统 100% 现行有效）。

### 4.4 结论四态

| 状态 `status_key` | 含义 |
|---|---|
| `CONFIRMED` | 已确认（含无金额的资格/合规类确认） |
| `SCENARIO` | 情景测算（非结论） |
| `DATA_GAP` | 证据不足 |
| `NOT_RECOMMEND` | 不推荐 |

### 4.5 七节点证据链

```
① 企业事实 → ② 诊断信号 → ③ 税务方向 → ④ 政策依据
→ ⑤ 条件核验 → ⑥ 税额计算 → ⑦ 最终结论
（Red / Blue 作为旁支审查说明）
```
证据链在前端完整呈现，并可导出 Markdown / PDF。

---

## 5. 数据与知识库

| 项 | 规模 |
|---|---|
| 财务数据 | 88 张 CSMAR 表；主表 714 字段；企业画像 845 字段；5,070 家公司、23,284 公司·年（2020–2024） |
| 政策库 | 5,593 条（国家税务总局政策法规库） |
| 案例库 | 1,021 篇（税屋网） |
| 检索 | 向量 + BM25 混合召回 + Reranker 重排 |

数据来源与合规详见 `数据来源与合规说明.md`。

---

## 6. 工程实现

### 6.1 后端 / 前端

- **后端**：FastAPI，REST 接口，OpenAPI 自动文档；前端 TypeScript 类型由 `scripts/gen_api_types.py` 从 OpenAPI 生成。
- **前端**：React + TypeScript + Vite；三栏工作台、异步任务、审查界面、帮助文档、设置页。

### 6.2 工程护栏

| 护栏 | 作用 |
|---|---|
| Embedding 指纹 | 模型变更时强制重建向量库，避免"静默失效" |
| Rerank 退避 / 冷却 / 缓存 | 限流时回退 RRF，保证可用性 |
| 版本指纹 | 数据 / 模型 / Skill / 配置的版本一致性校验 |
| 任务持久化 | 异步任务落盘，服务重启不丢失 |
| 结果缓存 | 按输入指纹复用分析结果，节省 token |
| 配置热失效 | 配置按 mtime 自动失效，无需重启 |

### 6.3 报告与导出

- 报告（Markdown / PDF）、证据链（Markdown / PDF）、意见书（Markdown / PDF）；
- **所有产出标注「AI 生成」声明**（报告正文、PDF 每页页脚、前端页脚）。

---

## 7. 测试与效果评估

### 7.1 回归测试

| 套件 | 用例数 | 通过率 |
|---|---|---|
| Skill 两层测试（Calculator + Decision） | 147 | 100% |
| Golden Cases（业务语义） | 5 | 100% |
| 模块测试 | 23 | 100% |
| **合计** | **175** | **100%** |

### 7.2 效果评估（详见 `效果评估报告.md`）

在同一份企业数据下，与"大模型 + 政策检索"对比（30 家真实公司）：

| 指标 | 本系统 | 大模型 + 政策检索 |
|---|---|---|
| 证据接地率 | **100%** | 0% |
| 政策可核验率 | **100%** | 72% |
| 政策现行有效率 | **100%** | 72% |
| 建议适用率 | **100%** | 98.1% |

**独立有效性**：系统仅用 2024 数据，命中 2025 年真实事件方向 **50%（15/30）**，其中可由财报推断的方向达 **75–86%**。

---

## 8. 合规与伦理

| 项 | 做法 |
|---|---|
| 密钥 | `.env` 加载，`.env.example` 模板，密钥不落盘 |
| AI 声明 | 所有输出标注「AI 生成」 |
| 可解释性 | 7 节点证据链 + 政策时效 + 免责声明 |
| 负面评价 | 只做事实性观察，结论保守 |
| 数据合规 | 详见 `数据来源与合规说明.md`（含风险提示与整改建议） |

---

## 9. 复现与运行

```bash
# 1) 安装依赖
pip install -r requirements.txt          # 或 pip install -e .

# 2) 配置密钥
cp .env.example .env                     # 填入 DeepSeek / SiliconFlow Key

# 3) 启动（单源，前端已构建）
python run_server.py                     # 打开 http://127.0.0.1:8000
# Windows 亦可双击「启动税务分析.bat」

# 4) 测试
pytest -q                                # 175 用例
```

---

## 10. 局限与展望

**局限**
1. 财务数据为**年度**口径，无法刻画季度内的时点状态；
2. 政策适用性元数据（地域 / 行业 / 条件）尚不完整；
3. 效果评估样本规模有限（30 真阳 + 59 对照）；
4. 事件驱动型方向（股权激励、组织架构）难以由财报推断。

**展望**
1. 补充政策适用性结构化元数据；
2. 引入季度数据与更多外部事件；
3. 扩展计算正确性的专项评估；
4. 为更多行业定制口径与阈值。
