"""统一的「AI 生成」声明与免责提示（竞赛伦理合规要求）。

竞赛手册要求：**智能体输出中必须包含「AI 生成」声明**，避免误导使用者
将其当作专业意见。此模块集中定义文案，供报告 / 证据链 / 意见书 / 前端共用，
保证口径一致、改一处即全局生效。
"""
from __future__ import annotations

# 简短徽标（UI / 报告标题旁）
AI_BADGE = "AI 生成"

# 完整声明（正文/导出文件顶部与末尾）
AI_NOTICE = (
    "本内容由人工智能（AI）自动生成，基于公开结构化数据与公开政策法规，"
    "仅供研究与辅助决策参考，不构成税务、审计、法律等专业意见或纳税申报依据；"
    "请以官方文件及具备资质的专业人员判断为准。"
)

# 报告开头的一句话标注
AI_NOTICE_SHORT = "本报告由 AI 自动生成，仅供参考，不构成专业意见或申报依据。"


def markdown_notice(level: str = "> ") -> str:
    """返回用于 Markdown 的声明行（默认引用块样式）。"""
    return f"{level}⚠️ **{AI_BADGE}**｜{AI_NOTICE}"
