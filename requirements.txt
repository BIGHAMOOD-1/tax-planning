# ===== 智能税务筹划系统 · Python 依赖 =====
# 安装：pip install -r requirements.txt   （建议 Python 3.11）

# —— 数据层 ——
pandas>=2.0
openpyxl>=3.1
pyarrow>=14.0
PyYAML>=6.0
numpy>=1.24

# —— 系统架构 / RAG ——
requests>=2.28
scikit-learn>=1.3
jieba>=0.42
joblib>=1.3
rank_bm25>=0.2

# —— 证据层（PDF 抽取）——
pdfplumber>=0.11
PyMuPDF>=1.24

# —— 环境变量（.env 加载）——
python-dotenv>=1.0

# —— UI / API ——
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
python-multipart>=0.0.9

# —— 报告导出（PDF）——
reportlab>=4.0

# —— 可选（重新生成图标时需要）——
# Pillow>=10.0

# —— 可选（历史/扩展用，非必需）——
# datasets>=2.18
# huggingface_hub>=0.20
